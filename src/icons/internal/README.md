# Internal Icon Pack

- One TSX file per icon under `icons/`
- Outline and filled variants for each icon
- Standard sizes in `BaseIcon.tsx`: xs=12, sm=16, md=20, lg=24, xl=32
- All icons use `currentColor`
- All icons use `viewBox="0 0 24 24"`
